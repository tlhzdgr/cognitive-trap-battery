// Optional deployment settings. GitHub Pages works with these defaults.
window.EXPERIMENT_CONFIG = {
  studyId: "visual-reasoning-seven-item-demo",
  dataEndpoint: "",        // Optional HTTPS endpoint accepting POSTed JSON.
  completionUrl: "",       // Optional redirect after successful completion.
  showDownloadButtons: true,
  shuffleTasks: true,
  shuffleChoices: true
};
